SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

0 0 * * 0     postgres pgbackrest --type=full --stanza=ats --repo=1 backup
0 0 * * 1-6   postgres pgbackrest --type=diff --stanza=ats --repo=1 backup
# 30 0 * * 0    postgres pgbackrest --type=full --stanza=ats --repo=2 backup
# 30 0 * * 1-6  postgres pgbackrest --type=diff --stanza=ats --repo=2 backup

